[package]
name = "ns-channel-cli"
version = "0.1.0"
edition = "2021"

[lib]
name = "nschannel_cli"

[dependencies]
ns-core = { path = "../core" }
async-trait = { workspace = true }
tokio = { workspace = true }
